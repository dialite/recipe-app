<p style="color: green"><%= notice %></p>

<h1>Recipefoods</h1>

<div id="recipefoods">
  <% @recipe_foods.each do |recipefood| %>
    <%= render recipefood %>
    <p>
      <%= link_to "Show this recipefood", recipe_food %>
    </p>
  <% end %>
</div>

<%= link_to "New recipefood", new_recipe_food_path %>
